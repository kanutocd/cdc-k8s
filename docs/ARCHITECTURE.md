# Architecture

- Describe the key components: Helm charts, KEDA scaling, reusable GitHub workflows, project templates.
- Explain how these components interact—how standards, CI, and deployment artifacts support any CDC project.
- Highlight the architecture’s flexibility: add new standards or deployment models over time.
